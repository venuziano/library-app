# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type PageInfoGQL {
  totalItems: Int!
  totalPages: Int!
  currentPage: Int!
}

type AuthorGQL {
  createdAt: DateTime!
  updatedAt: DateTime!
  deletedAt: DateTime
  id: ID!
  firstname: String!
  lastname: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type PaginatedAuthorsGQL {
  items: [AuthorGQL!]!
  pageInfo: PageInfoGQL!
}

type CacheEntry {
  key: String!
  value: String
}

type CategoryGQL {
  createdAt: DateTime!
  updatedAt: DateTime!
  deletedAt: DateTime
  id: ID!
  name: String!
}

type PaginatedCategoriesGQL {
  items: [CategoryGQL!]!
  pageInfo: PageInfoGQL!
}

type Query {
  getAllAuthors(limit: Int! = 50, page: Int! = 1, sort: String! = "createdAt", searchTerm: String, order: SortOrder! = DESC): PaginatedAuthorsGQL!
  getAuthorById(id: ID!): AuthorGQL
  getAllCategories(limit: Int! = 50, page: Int! = 1, sort: String! = "createdAt", searchTerm: String, order: SortOrder! = DESC): PaginatedCategoriesGQL!
  getCategoryById(id: ID!): CategoryGQL
  cacheKeys: [CacheEntry!]!
}

"""Sort direction (ascending or descending)"""
enum SortOrder {
  ASC
  DESC
}

type Mutation {
  createAuthor(input: CreateAuthorInput!): AuthorGQL!
  updateAuthor(input: UpdateAuthorInput!): AuthorGQL!
  patchAuthor(input: PatchAuthorInput!): AuthorGQL!
  deleteAuthor(id: Int!): AuthorGQL!
  createCategory(input: CreateCategoryInput!): CategoryGQL!
  updateCategory(input: UpdateCategoryInput!): CategoryGQL!
  patchCategory(input: PatchCategoryInput!): CategoryGQL!
  deleteCategory(id: Int!): CategoryGQL!
  setCache(input: String!): Boolean!
}

input CreateAuthorInput {
  firstname: String!
  lastname: String!
}

input UpdateAuthorInput {
  firstname: String!
  lastname: String!
  id: Int!
}

input PatchAuthorInput {
  firstname: String
  lastname: String
  id: Int!
}

input CreateCategoryInput {
  name: String!
}

input UpdateCategoryInput {
  name: String!
  id: Int!
}

input PatchCategoryInput {
  name: String
  id: Int!
}